# img2svg 环境变量
# 复制为 .env 并填写

# 服务端口
PORT=4003

# 数据目录（SQLite + 临时文件）
DATA_DIR=./data

# trans (pdf-translate-web) 的数据库路径，用于共享用户认证
TRANS_DB_PATH=./data/app.db

# vtracer 二进制路径
VTRACER_PATH=./bin/vtracer

# Session 签名密钥，至少 32 字符
COOKIE_SECRET=replace-with-at-least-32-random-characters
